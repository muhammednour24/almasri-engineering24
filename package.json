{
  "name": "engineering-portal",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/local-server.mjs",
    "build": "wrangler deploy --dry-run --outdir build",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.js",
    "init-owner": "node scripts/init-owner.mjs",
    "dev:cloudflare": "wrangler dev"
  },
  "dependencies": {
    "mongodb": "6.16.0",
    "punycode": "2.3.1"
  },
  "devDependencies": {
    "wrangler": "4.20.0"
  }
}
