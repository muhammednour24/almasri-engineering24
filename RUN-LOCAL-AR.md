# تشغيل النسخة المصححة محليًا

نجح فحص MongoDB المباشر على جهازك، بينما فشل الطلب عبر Wrangler.
هذه النسخة تجعل `npm run dev` يشغل الموقع بواسطة Node.js مباشرة، مع نفس قاعدة البيانات ونفس وظائف الموقع.
هذا إصلاح لمسار التشغيل المحلي؛ لا يؤكد إصلاح النسخة المنشورة على Cloudflare أو يحدد العطل الداخلي في Wrangler.

1. فك ضغط المشروع في مجلد جديد.
2. افتح Terminal داخل مجلد `33333333`.
3. شغّل `npm ci` ثم `npm run dev`.
4. افتح http://127.0.0.1:8787 واترك التيرمينال مفتوحًا.
5. يمكنك فحص http://127.0.0.1:8787/api/status؛ النتيجة الناجحة تحتوي `database: true`.

يقرأ التشغيل المحلي إعدادات `.dev.vars` الموجودة في المشروع. لا تشارك هذا الملف علنًا.
يحتفظ المشروع بإعداداتك الأصلية؛ لا يحذف أو يعيد تهيئة البيانات.
`START-HERE.html` ما زال معاينة تجريبية. استخدم رابط الخادم للعمل الحقيقي.
للعودة إلى اختبار Cloudflare استخدم `npm run dev:cloudflare`.
أمرا `npm run build` و`npm run deploy` ما زالا خاصين بـCloudflare.
الخادم الجديد محلي فقط ويستمع على 127.0.0.1؛ ليس خادم نشر على الإنترنت.

إذا كان المنفذ مشغولًا، أوقف نافذة التشغيل القديمة باستخدام Ctrl+C.
